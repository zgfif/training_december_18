require 'rails_helper'

RSpec.describe Backoffice::SessionPolicy, type: :policy do
  # let(:record) { Backoffice::Session.new email: nil, password: nil }

  # subject { described_class }

  # permissions :create? do
  #   context do
  #
  #     let(:current_user) { stub_model User, roles: :administrator }
  #
  #     before { expect(:record).to receive(:user).and_return(:current_user) }
  #     it do
  #       should permit record
  #
  #     end
  #   end
    #
    # context do
    #   let(:user) { stub_model User, roles: :user }
    #
    #   it { should_not permit record }
    # end
  # end
end
